# NRK Radio Transcriber Dependencies
# Note: pyproject.toml is the primary dependency source.
# This file is provided for convenience with pip install -r.

# Audio processing
faster-whisper>=1.0.0  # Primary transcription engine (CTranslate2-based)

# HTTP requests
requests>=2.31.0

# CLI and configuration
click>=8.1.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
rich>=13.7.0

# Database for storing transcriptions
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Optional: OpenAI Whisper fallback (not required if using faster-whisper)
# openai-whisper>=20231117

# Optional: YouTube support
# yt-dlp>=2024.0.0

# Optional: Speaker diarization
# pyannote.audio>=3.1.0
# torch>=2.0.0
